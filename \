---
- hosts: kube-cluster
  become: true
  tasks:
    - name: criando diretórios
      ansible.builtin.file:
        path: "{{ item }}"
        mode: "1777"
        state: directory
      loop:
        - /opt/nfs
        - /opt/dados
        - /opt/dados/mysql
        - /opt/storage
        - /opt/storage/default
        - /opt/storage/local
      
